# unconventional-moves - baseline coverage audit (2026-09-23)

Branch: `imp/portfolio-triage-phase13-2026-09-23`
Default branch captured at clone: `main`
Python: Python 3.12.10

## Coverage capability detection

| check | result |
|---|---|
| `coverage` importable in active interpreter | no |
| `pytest-cov` importable in active interpreter | no |
| declared in `pyproject.toml` / `requirements*.txt` / `pytest.ini` / `.coveragerc` | no |
| install `coverage` + `pytest-cov` via pip | skipped (heavy-install rule) |

Action taken: **no-coverage-tool** (install skipped per heavy-install rule).

## Source layout surveyed

| target | py files | tests dir | python test runner declared |
|---|---:|---|---|
| repo root | n/a | tests/ | none |

Counts above come from a recursive glob of `*.py`; per-subpackage counts were not enumerated because the no-coverage-tool verdict applies uniformly.

## Notes

- Per the portfolio triage plan, this commit is a no-op audit-clear baseline. No source, test, or schema files were modified.
- No PR opened by this pass; the human will open the PR on the GitHub web UI.
- Default branch is untouched. Branch push: `unconventional-moves` @ `imp/portfolio-triage-phase13-2026-09-23`.
- Heavy-install rule honoured: `pip install coverage pytest-cov` was not invoked.

## Verdict

No coverage tool available in the active interpreter, and none is declared in repo config. Coverage baseline cannot be captured without installing new packages, which the heavy-install rule forbids. Follow-up: decide whether to add `coverage` and `pytest-cov` to a repo-level dev dependency group in a future pass.
